<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/menu_enviar_notas"
        android:title="@string/enviar"
        android:icon="@mipmap/ic_enviar"
        app:showAsAction="always"/>

    <item android:id="@+id/menu_baixar_provas"
        android:title="@string/baixar_provas"
        android:icon="@mipmap/ic_receber"
        app:showAsAction="always"/>
    <item android:id="@+id/menu_mapa"
        android:title="@string/visualizar_mapa"
        android:icon="@mipmap/ic_mapa"
        app:showAsAction="always"/>

</menu>